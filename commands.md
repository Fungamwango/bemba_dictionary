#Deployed. The command I use to deploy is:
npx wrangler pages deploy . --project-name=bemba-dictionary --commit-dirty=true

#deploys the current directory (.) to Cloudflare Pages
npx wrangler pages deploy .
